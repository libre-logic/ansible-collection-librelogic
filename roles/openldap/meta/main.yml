---

galaxy_info:
  role_name: openldap
  author: "Libre Logic"
  description: "OpenLDAP server + web administration tools (LAM/SSP)"
  license: "GPL-3.0"
  min_ansible_version: 2.7
  platforms:
    - name: Debian
      versions:
        - stretch
  galaxy_tags:
    - ldap
    - directory
    - authentication

dependencies:
  - librelogic.librelogic.common

